/** Shared labels for DSH Markdown primitives rendered by BranchMark. */

import type { MarkdownLabels } from '@deepseek-ai/dsh-client-ui-primitives'

/** Chinese copy owned by the current BranchMark browser experience. */
export const BRANCHMARK_MARKDOWN_LABELS: MarkdownLabels = {
  code: { copyLabel: '复制', copiedLabel: '已复制' },
  footnotes: '脚注',
}
