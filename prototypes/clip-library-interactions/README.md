# PROTOTYPE — 枝签库交互

问题：多选批量操作、窄屏响应、长文本查看、拖拽排序与置顶应该如何组合。

这是一次性 UI 原型，不连接 BranchMark 数据，也不应作为生产实现合并。

```sh
node prototypes/clip-library-interactions/server.mjs
```

打开 `http://127.0.0.1:4178/?variant=A`，底部切换器可以在 A/B/C 三种方案间切换。
